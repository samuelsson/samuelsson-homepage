import styles from './BlogPostFooter.module.scss';

const BlogPostFooter = (): JSX.Element | null => {
  return <footer className={styles.BlogPostFooter}>footer</footer>;
};

export default BlogPostFooter;
