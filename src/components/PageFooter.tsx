import { ReactElement } from 'react';

const PageFooter = (): ReactElement => {
  return (
    <footer className="bg-slate-50 border-t-2 border-t-slate-100">
      <div className="container-xl py-3">Footer</div>
    </footer>
  );
};

export default PageFooter;
